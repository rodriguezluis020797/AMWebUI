import { IdentityPingDirective } from './identity-ping.directive';

describe('IdentityPingDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityPingDirective();
    expect(directive).toBeTruthy();
  });
});

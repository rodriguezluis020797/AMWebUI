export enum HttpStatusCodeEnum {
  Unknown = 0,
  Success = 200,
  BadCredentials = 400,
  Unauthorized = 401,
  BadPassword = 402,
  ServerError = 500,
  SystemUnavailable = 501,
}

export enum CookieEnum {
  Unknown = 0,
}
